---
title: 生成的 API 参考
description: 候选重新生成 SDK Javadoc 参考的状态。
---

本页是待发布的候选文档。当前托管的 Javadoc 已陈旧，不得将其视为当前公共 API 契约。

## 重新生成状态

候选重新生成目前受以下三项问题阻塞：

- 某个生成类型虽然在候选输出中可公开搜索，但其自述为内部且非稳定。
- 某个生成服务仍标记为 Preview，其公开参考策略仍需确认。
- 候选输出仍带有 `0.42.0-SNAPSHOT` 版本标识。

在这些阻塞问题解决之前，托管的 HTML 参考仍然过时。不要依赖它来作出发布、部署或兼容性声明。

## 谨慎使用可用指南

- 请阅读 [SDK API 指南](./sdk-api) 以了解精选边界。
- 请阅读[构建插件](../build-a-plugin/overview)以了解候选扩展路径。
- 请将每项当前 API 说明视为与 Preview 和 capability 相关。

本页不提供本地重新生成流程。刷新后的生成参考资料仍待发布。
