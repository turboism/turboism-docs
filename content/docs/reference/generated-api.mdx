---
title: Generated API Reference
description: SDK Javadoc generation, review, and publication status.
---

The SDK Javadoc is generated from the matching Turboism source tree and hosted under the SDK documentation site only after review.

## Documentation update workflow

When the public SDK, its documentation, or its version changes:

1. generate Javadoc with the source repository's `:sdk:javadoc` Gradle task;
2. verify the exact version and reject snapshot output;
3. review every publicly searchable package and type;
4. confirm that internal implementation types and unapproved Preview APIs are not presented as public contracts;
5. check generated text for private data, local paths, credentials, host logs, or implementation-only evidence;
6. synchronize the reviewed output to the documentation site; and
7. run the documentation lint, type check, and production build.

Successful generation alone does not approve publication. If the generated public surface or version is not ready, keep the hosted Javadoc unchanged and record the blocker here.

## Current status

The latest reviewed candidate was not synchronized because:

- a generated type describes itself as internal and non-stable while remaining publicly searchable;
- a generated service remains marked Preview and still needs an approved public-reference policy; and
- the reviewed candidate used the wrong version identifier.

Until those blockers are resolved, use the [SDK API Guide](./sdk-api) and the public SDK source declarations. Do not rely on the currently hosted HTML reference for release, deployment, or compatibility claims.
